// Simple test to verify Jest is working
describe('Simple Test', () => {
  test('should pass', () => {
    expect(1 + 1).toBe(2);
  });

  test('should handle basic math', () => {
    expect(2 * 3).toBe(6);
  });
});
